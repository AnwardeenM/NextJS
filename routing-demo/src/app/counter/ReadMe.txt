This Folder and component has been created to learn about the Use client feature and Metadata Which can't be used together.

It is better to use in the following ways of this component.

Create an page and component.tsx files separate 

And the Use client inside the component.tsx file

call the component in the page file, where you can use the metadata object.

Note: It only works this way not the either way.