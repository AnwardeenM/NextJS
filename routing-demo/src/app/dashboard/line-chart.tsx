export default function LineChart(){
    return <h1>Line Chart details!</h1>
}

// This page is created as an example for the file-coloaction feature of NextJs.
// Since we know that NextJs runs based file structure.
// If we didn't mention the page.tsx/.js in our folder it won't recognize any file inside that Folder/File Location.

// However when we start mentioning the page.tsx file it starts to recognize.