export default function NotFound(){
    return(
        <div>
            <h2>Page Not Found!</h2>
            <p>Requested Source isn't found</p>
        </div>        
    ) 
}

//This NotFound function doesn't accept any parameters.
//That's default in the Nextjs function